[project]
name = "stone-agent-core"
version = "0.1.0"
description = "Core registry and workflow execution engine for Stone."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]